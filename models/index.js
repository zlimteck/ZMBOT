/* Importing the `guild.js` and `member.js` files. */
module.exports = {
    Guild: require("./guild"),/* Importing the `guild.js` file. */
    Member: require("./member") /* Importing the `guild.js` file. */
};